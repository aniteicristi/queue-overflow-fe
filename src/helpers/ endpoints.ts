export enum Endpoints {
  getQuestions = "/questions",
  getSelf = "/auth/self",
  postQuestion = "/questions",
  postAnswer = "/answers",
  voteQuestion = "/votes/question",
  voteAnswer = "/votes/answer",
  getVoteStatusQuestion = "/votes",
  getVoteStatusAnswer = "/votes",
}
